program nomor5;
uses crt;

var
    i,jumlah,N : integer;
    //misal user akan input jumlah bilangan (N) tidak lebih dari 100
    data:array [1..100] of integer;

begin
    clrscr;
    // jumlah:= 0;
    // write('Masukkan jumlah bilangan N: ');readln(N);

    // for i:= 1 to N do
    //     begin
    //         write('Masukkan bilangan ke-',i,' :');readln(data[i]);
    //         if data[i] mod 2 = 0 then
    //             jumlah:= jumlah + data[i];
    //     end;
    
    // writeln('Jumlah bilangan genap: ', jumlah);
    // readln;

end.


